import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#5ee25c',
        },
        secondary: {
            main: '#f50057',
        },
    },
});

export default theme;
